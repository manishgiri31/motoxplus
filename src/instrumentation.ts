// TEMPORARY LOCAL-ONLY STUB — not committed, restored from .scratch/instrumentation.ts.bak
// before this task finishes. The real file's dynamic import of '@/lib/redis' breaks Edge
// bundling for every middleware-matched route (see conversation), which is a pre-existing
// backend issue out of scope for this frontend-only redesign. This stub exists solely so
// dev-mode auth-gated pages can be screenshotted/verified locally.
export async function register() {}
